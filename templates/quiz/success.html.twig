{% extends 'base.html.twig' %}

{% block title %}Quiz erfolgreich - {{ game.name }}{% endblock %}

{% block body %}
<div class="quiz-mobile min-h-screen flex items-center justify-center p-4">
    <div class="quiz-card w-full max-w-md p-6 text-center">
        <div class="text-6xl mb-4">🎉</div>
        <h1 class="text-2xl font-bold text-gray-800 mb-4">Erfolgreich abgeschickt!</h1>
        <p class="text-gray-600 mb-6">
            Danke {{ player.name }}, deine Antworten für "{{ game.name }}" wurden gespeichert.
        </p>
        
        <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
            <h3 class="text-lg font-semibold text-green-800 mb-2">Was passiert jetzt?</h3>
            <p class="text-green-700 text-sm">
                Warte bis alle Spieler geantwortet haben. Dann werden die Ergebnisse berechnet und die Punkte vergeben.
            </p>
        </div>
        
        <div class="text-sm text-gray-600">
            Du kannst diese Seite jetzt schließen oder zum nächsten Spiel wechseln.
        </div>
    </div>
</div>
{% endblock %}