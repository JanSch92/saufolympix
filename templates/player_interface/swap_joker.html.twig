<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Punkte tauschen - {{ player.name }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        
        .player-card {
            background: white;
            border-radius: 1rem;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }
        
        .player-option, .game-option {
            background: white;
            border: 2px solid #e5e7eb;
            border-radius: 1rem;
            padding: 1rem;
            margin-bottom: 1rem;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .player-option:hover, .game-option:hover {
            border-color: #3b82f6;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }
        
        .player-option.selected, .game-option.selected {
            border-color: #10b981;
            background: #ecfdf5;
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
        }
        
        .game-option.has-swap {
            border-color: #ef4444;
            background: #fef2f2;
            opacity: 0.7;
            cursor: not-allowed;
        }
        
        .game-option.has-swap:hover {
            transform: none;
            box-shadow: none;
        }
        
        .btn-swap {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            border: none;
            padding: 1rem 2rem;
            border-radius: 0.5rem;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            width: 100%;
        }
        
        .btn-swap:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.4);
        }
        
        .btn-swap:disabled {
            background: #d1d5db;
            color: #6b7280;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }
        
        .points-diff {
            font-size: 0.9rem;
            font-weight: 600;
            padding: 0.25rem 0.5rem;
            border-radius: 0.375rem;
        }
        
        .points-diff.positive {
            background: #dcfce7;
            color: #166534;
        }
        
        .points-diff.negative {
            background: #fef2f2;
            color: #dc2626;
        }
        
        .points-diff.neutral {
            background: #f3f4f6;
            color: #6b7280;
        }
        
        .warning-box {
            background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
            border: 1px solid #f59e0b;
            border-radius: 0.75rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .info-box {
            background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%);
            border: 1px solid #3b82f6;
            border-radius: 0.75rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }

        .step {
            opacity: 0.5;
            transition: opacity 0.3s ease;
        }
        
        .step.active {
            opacity: 1;
        }
    </style>
</head>
<body class="p-4">
    <div class="max-w-2xl mx-auto space-y-4">
        <!-- Header -->
        <div class="player-card p-6">
            <div class="text-center">
                <h1 class="text-2xl font-bold text-gray-800">⇄ Punkte tauschen</h1>
                <p class="text-gray-600 mt-1">{{ player.name }} - {{ olympix.name }}</p>
            </div>
        </div>
        
        <!-- Info Box -->
        <div class="info-box">
            <h3 class="font-semibold text-blue-800 mb-2">ℹ️ Wie funktioniert der Tausch?</h3>
            <ul class="text-blue-700 text-sm space-y-1">
                <li>• Wähle zuerst ein <strong>zukünftiges Spiel</strong> aus (noch nicht gespielt)</li>
                <li>• Wähle dann einen <strong>Spieler</strong> mit dem du tauschen möchtest</li>
                <li>• Der Tausch wird <strong>vorgemerkt</strong> für dieses Spiel</li>
                <li>• <strong>Wenn das Spiel gespielt wird</strong>, werden eure Positionen getauscht</li>
                <li>• Pro Spiel ist nur <strong>EIN Tausch</strong> möglich - wer zuerst kommt, mahlt zuerst!</li>
                <li>• Der Joker kann nur <strong>einmal pro Olympix</strong> verwendet werden</li>
            </ul>
        </div>

        {% if available_games_with_status is empty %}
            <div class="player-card p-6">
                <div class="text-center text-gray-500 py-8">
                    <div class="text-4xl mb-4">🚫</div>
                    <p class="text-lg">Keine Spiele verfügbar</p>
                    <p class="text-sm mt-2">
                        Alle zukünftigen Spiele haben bereits einen vorgemerkten Punkte-Tausch oder es gibt keine zukünftigen Spiele
                    </p>
                </div>
                
                <div class="mt-6">
                    <a href="{{ path('app_player_dashboard', {olympixId: olympix.id, playerId: player.id}) }}" 
                       class="block w-full text-center py-3 px-4 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors">
                        ← Zurück zum Dashboard
                    </a>
                </div>
            </div>
        {% else %}
            <form action="{{ path('app_player_joker_swap', {olympixId: olympix.id, playerId: player.id}) }}" method="POST" id="swapForm">
                
                <!-- Step 1: Game Selection -->
                <div class="player-card p-6 step active" id="step1">
                    <h2 class="text-xl font-bold text-gray-800 mb-4">🎮 Schritt 1: Zukünftiges Spiel auswählen</h2>
                    
                    <div class="space-y-3" id="gameOptions">
                        {% for gameData in available_games_with_status %}
                            <label class="game-option block {% if gameData.has_swap %}has-swap{% endif %}" 
                                   for="game_{{ gameData.game.id }}"
                                   {% if gameData.has_swap %}title="Für dieses Spiel wurde bereits ein Tausch vorgemerkt"{% endif %}>
                                <input type="radio" 
                                       id="game_{{ gameData.game.id }}"
                                       name="selected_game_id" 
                                       value="{{ gameData.game.id }}"
                                       class="hidden"
                                       {% if gameData.has_swap %}disabled{% endif %}
                                       onchange="selectGame(this, {{ gameData.game.id }})">
                                
                                <div class="flex items-center justify-between">
                                    <div class="flex-1">
                                        <div class="font-semibold text-gray-800 text-lg">{{ gameData.game.name }}</div>
                                        <div class="text-sm text-gray-600">{{ gameData.game.gameTypeLabel }} • {{ gameData.game.statusLabel }}</div>
                                        {% if gameData.has_swap %}
                                            <div class="text-sm text-red-600 font-medium mt-1">❌ Tausch bereits vorgemerkt</div>
                                        {% else %}
                                            <div class="text-sm text-green-600 font-medium mt-1">✅ Verfügbar für Tausch</div>
                                        {% endif %}
                                    </div>
                                    
                                    <div class="text-right">
                                        <div class="text-2xl">
                                            {% if gameData.game.gameType == 'tournament_team' or gameData.game.gameType == 'tournament_single' %}
                                                🏆
                                            {% elseif gameData.game.gameType == 'quiz' %}
                                                🧠
                                            {% else %}
                                                🎯
                                            {% endif %}
                                        </div>
                                    </div>
                                </div>
                            </label>
                        {% endfor %}
                    </div>
                </div>

                <!-- Step 2: Player Selection -->
                <div class="player-card p-6 step" id="step2">
                    <h2 class="text-xl font-bold text-gray-800 mb-4">👤 Schritt 2: Tausch-Partner auswählen</h2>
                    <p class="text-gray-600 mb-4" id="selectedGameInfo">Wähle zuerst ein Spiel aus</p>
                    
                    <div class="space-y-3" id="playerOptions">
                        {% for availablePlayer in available_players %}
                            <label class="player-option block" 
                                   for="player_{{ availablePlayer.id }}"
                                   data-player-id="{{ availablePlayer.id }}">
                                <input type="radio" 
                                       id="player_{{ availablePlayer.id }}"
                                       name="target_player_id" 
                                       value="{{ availablePlayer.id }}"
                                       class="hidden"
                                       disabled
                                       onchange="selectPlayer(this)">
                                
                                <div class="flex items-center justify-between">
                                    <div class="flex-1">
                                        <div class="font-semibold text-gray-800 text-lg">{{ availablePlayer.name }}</div>
                                        <div class="text-sm text-gray-600">{{ availablePlayer.totalPoints }} Punkte gesamt</div>
                                        <div class="text-sm text-blue-600 mt-1">
                                            Wenn ihr beide spielt, werden eure Positionen getauscht
                                        </div>
                                    </div>
                                    
                                    <div class="flex items-center space-x-2">
                                        {% set diff = availablePlayer.totalPoints - player.totalPoints %}
                                        {% if diff > 0 %}
                                            <div class="points-diff positive">+{{ diff }} für dich möglich</div>
                                        {% elseif diff < 0 %}
                                            <div class="points-diff negative">{{ diff }} für dich möglich</div>
                                        {% else %}
                                            <div class="points-diff neutral">Gleich</div>
                                        {% endif %}
                                        <div class="text-2xl">⇄</div>
                                    </div>
                                </div>
                            </label>
                        {% endfor %}
                    </div>
                </div>
                
                <!-- Warning Box -->
                <div class="warning-box step" id="step3">
                    <h3 class="font-semibold text-amber-800 mb-2">⚠️ Wichtiger Hinweis</h3>
                    <ul class="text-amber-700 text-sm space-y-1">
                        <li>• Der Tausch wird <strong>vorgemerkt</strong> und kann nicht rückgängig gemacht werden</li>
                        <li>• Du kannst diesen Joker nur <strong>einmal verwenden</strong></li>
                        <li>• Pro Spiel ist nur <strong>ein Tausch möglich</strong></li>
                        <li>• Der Tausch erfolgt <strong>automatisch</strong> wenn das Spiel gespielt wird</li>
                        <li>• Wenn einer von euch nicht teilnimmt, <strong>verfällt der Tausch</strong></li>
                    </ul>
                </div>
                
                <!-- Submit Button -->
                <div class="player-card p-6 step" id="step4">
                    <div class="space-y-3">
                        <button type="submit" 
                                class="btn-swap" 
                                id="submitBtn"
                                disabled>
                            📝 Tausch vormerken
                        </button>
                        
                        <a href="{{ path('app_player_dashboard', {olympixId: olympix.id, playerId: player.id}) }}" 
                           class="block w-full text-center py-3 px-4 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors">
                            ← Zurück zum Dashboard
                        </a>
                    </div>
                </div>
            </form>
        {% endif %}
        
        <!-- Current Points Display -->
        <div class="player-card p-4">
            <h3 class="font-semibold text-gray-800 mb-3">📊 Deine aktuellen Punkte</h3>
            <div class="bg-blue-50 border border-blue-200 rounded-lg p-3">
                <div class="flex items-center justify-between">
                    <div class="font-medium text-blue-800">{{ player.name }}</div>
                    <div class="text-xl font-bold text-blue-900">{{ player.totalPoints }} Punkte</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        let selectedGameId = null;
        let selectedPlayerId = null;
        let gameData = {};
        
        // Game data from server - only basic info for pending games
        {% for gameData in available_games_with_status %}
            gameData[{{ gameData.game.id }}] = {
                name: "{{ gameData.game.name }}",
                gameType: "{{ gameData.game.gameType }}",
                hasSwap: {{ gameData.has_swap ? 'true' : 'false' }}
            };
        {% endfor %}
        
        function selectGame(radio, gameId) {
            selectedGameId = gameId;
            
            // Update UI
            document.querySelectorAll('.game-option').forEach(option => {
                option.classList.remove('selected');
            });
            radio.closest('.game-option').classList.add('selected');
            
            // Show step 2
            document.getElementById('step2').classList.add('active');
            
            // Update game info
            const game = gameData[gameId];
            document.getElementById('selectedGameInfo').textContent = 
                `Ausgewähltes Spiel: ${game.name} - Wenn das Spiel gespielt wird, werden die Positionen getauscht`;
            
            // Enable all player options for future games
            document.querySelectorAll('#playerOptions input[type="radio"]').forEach(input => {
                input.disabled = false;
                input.closest('.player-option').style.opacity = '1';
            });
            
            // Reset player selection
            selectedPlayerId = null;
            updateSubmitButton();
        }
        
        function selectPlayer(radio) {
            selectedPlayerId = radio.value;
            
            // Update UI
            document.querySelectorAll('.player-option').forEach(option => {
                option.classList.remove('selected');
            });
            radio.closest('.player-option').classList.add('selected');
            
            // Show remaining steps
            document.getElementById('step3').classList.add('active');
            document.getElementById('step4').classList.add('active');
            
            updateSubmitButton();
        }
        
        function updateSubmitButton() {
            const submitBtn = document.getElementById('submitBtn');
            
            if (selectedGameId && selectedPlayerId) {
                submitBtn.disabled = false;
                
                const game = gameData[selectedGameId];
                const playerName = document.querySelector(`input[value="${selectedPlayerId}"]`)
                    .closest('.player-option').querySelector('.font-semibold').textContent;
                
                submitBtn.textContent = `📝 Tausch für "${game.name}" mit ${playerName} vormerken`;
            } else {
                submitBtn.disabled = true;
                submitBtn.textContent = '📝 Tausch vormerken';
            }
        }
        
        // Form submission confirmation
        document.getElementById('swapForm').addEventListener('submit', function(e) {
            if (!selectedGameId || !selectedPlayerId) {
                e.preventDefault();
                alert('Bitte wähle sowohl ein Spiel als auch einen Spieler aus');
                return;
            }
            
            const game = gameData[selectedGameId];
            const playerName = document.querySelector(`input[value="${selectedPlayerId}"]`)
                .closest('.player-option').querySelector('.font-semibold').textContent;
            
            const confirmMessage = `Punkte-Tausch wirklich vormerken?

Spiel: ${game.name}
Tausch-Partner: ${playerName}

• Der Tausch wird vorgemerkt und erfolgt automatisch wenn das Spiel gespielt wird
• Wenn ihr beide am Spiel teilnehmt, werden eure Positionen getauscht
• Falls einer von euch nicht teilnimmt, verfällt der Tausch
• Dieser Joker kann nur einmal verwendet werden!

Soll der Tausch vorgemerkt werden?`;
            
            if (!confirm(confirmMessage)) {
                e.preventDefault();
            }
        });
    </script>
</body>
</html>